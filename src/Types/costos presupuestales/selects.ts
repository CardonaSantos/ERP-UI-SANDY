export interface PresupuestoPartidaSelect {
  id: number;
  montoDisponible: number;

  montoComprometido: number;
  partida: string;
  partidaId: number;
  fechaFin: string;
  fechaInicio: string;
}
